name: Build and release binary

on:
  workflow_dispatch:
    inputs:
      create_release:
        description: "Create a GitHub release and upload built assets"
        required: false
        type: boolean
        default: false
      release_tag:
        description: "Tag to release when manually dispatched (example: v0.6.0)"
        required: false
        type: string
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
  push:
    tags:
      - 'v*'

permissions:
  contents: read

env:
  BUILD_DIR: build-gha
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  preflight-guard-tests:
    name: Preflight guard tests
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run CI guard tests
        run: |
          set -eux
          python3 -m unittest discover -s misrc_tools/test -p 'test_ci_guards.py'
          python3 misrc_tools/test/ci_guard_tests.py
  preflight-cross-platform-guards:
    name: Cross-platform guard tests (${{ matrix.runner }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-22.04
            guard_mode: static
          - runner: windows-2022
            guard_mode: static
          - runner: macos-14
            guard_mode: runtime
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'

      - name: Run static CI guard tests
        if: matrix.guard_mode == 'static'
        run: |
          python misrc_tools/test/ci_guard_tests.py --static-only

      - name: Run runtime CI guard tests
        if: matrix.guard_mode == 'runtime'
        run: |
          python misrc_tools/test/ci_guard_tests.py
  linux-appimage:
    name: Linux AppImage build (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-22.04
            linuxdeploy_arch: x86_64
            artifact_suffix: x86
          - arch: arm64
            runner: ubuntu-22.04-arm
            linuxdeploy_arch: aarch64
            artifact_suffix: arm64
    runs-on: ${{ matrix.runner }}
    needs:
      - preflight-guard-tests
      - preflight-cross-platform-guards
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Cache vendored deps (.deps/install)
        id: deps-cache
        uses: actions/cache@v6
        with:
          path: .deps/install
          key: deps-linux-${{ matrix.arch }}-raylib5.5-libuvc-v0.0.7-flac1.5.0-${{ hashFiles('third_party/hsdaoh/**') }}

      - name: Resolve build version
        id: version
        env:
          GITHUB_REF: ${{ github.ref }}
          CI_EVENT_NAME: ${{ github.event_name }}
          CI_CREATE_RELEASE: ${{ github.event.inputs.create_release }}
          CI_RELEASE_TAG: ${{ github.event.inputs.release_tag }}
        run: |
          set -eux
          VERSION="$(misrc_tools/ci-resolve-version.sh)"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Install build dependencies
        run: |
          set -eux
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            curl \
            git \
            imagemagick \
            libasound2-dev \
            libfftw3-dev \
            libgl1-mesa-dev \
            libusb-1.0-0-dev \
            libuvc-dev \
            libsoxr-dev \
            libx11-dev \
            libxcursor-dev \
            libxi-dev \
            libxinerama-dev \
            libxrandr-dev \
            meson \
            nasm \
            ninja-build \
            patchelf \
            pkg-config \
            unzip \
            zip

      - name: Fetch libFLAC 1.5.0 cache + build hsdaoh/raylib
        run: |
          set -eux
          DEPS_PREFIX="$PWD/.deps/install"
          mkdir -p .deps

          # Cache hit guard: if .deps/install was restored from actions/cache
          # (key includes hashFiles(third_party/hsdaoh/**) + raylib/libuvc/flac
          # versions), skip the FLAC/hsdaoh/raylib rebuild entirely. A source
          # change invalidates the cache key and forces a clean rebuild.
          if [[ -f "$DEPS_PREFIX/lib/pkgconfig/hsdaoh.pc" ]]; then
            echo "deps cache hit - skipping hsdaoh/raylib/flac rebuild"
            PKG_CONFIG_PATH="${DEPS_PREFIX}/lib/pkgconfig:${DEPS_PREFIX}/lib64/pkgconfig" \
              pkg-config --modversion flac
            exit 0
          fi

          # libFLAC 1.5.0 is required by misrc_tools/meson.build for the
          # multithreaded encoder (FLAC__stream_encoder_set_num_threads, libFLAC
          # API v14). Ubuntu 22.04's libflac-dev apt is stuck at 1.3.3, so fetch a
          # prebuilt static lib from the harrypm/MISRC-ci-cache release (built on
          # jammy/glibc-2.35, ABI-compatible with the runners). The arm64 prebuilt
          # is produced by the one-off .github/workflows/flac-cache.yml job; until
          # it exists the arm64 job falls back to building from the cached source
          # tarball, so arm64 stays unblocked without the cache.
          FLAC_VERSION="1.5.0"
          CACHE_BASE="https://github.com/harrypm/MISRC-ci-cache/releases/download/flac-${FLAC_VERSION}-linux"
          FLAC_ARCH="${{ matrix.arch }}"
          PREBUILT_NAME="flac-${FLAC_VERSION}-prebuilt-linux-${FLAC_ARCH}.tar.xz"
          PREBUILT_SHA_NAME="${PREBUILT_NAME}.sha256"
          SRC_NAME="flac-${FLAC_VERSION}.tar.xz"
          FLAC_SRC_SHA256="f2c1c76592a82ffff8413ba3c4a1299b6c7ab06c734dee03fd88630485c2b920"

          if curl -fsSL -o "/tmp/${PREBUILT_NAME}" "${CACHE_BASE}/${PREBUILT_NAME}" \
             && curl -fsSL -o "/tmp/${PREBUILT_SHA_NAME}" "${CACHE_BASE}/${PREBUILT_SHA_NAME}"; then
            expected="$(awk '{print $1}' "/tmp/${PREBUILT_SHA_NAME}")"
            actual="$(sha256sum "/tmp/${PREBUILT_NAME}" | awk '{print $1}')"
            [[ "${actual}" == "${expected}" ]] || { echo "prebuilt libFLAC sha mismatch: expected ${expected} got ${actual}"; exit 1; }
            rm -rf .deps/flac-prebuilt
            mkdir -p .deps/flac-prebuilt "${DEPS_PREFIX}/lib/pkgconfig" "${DEPS_PREFIX}/include"
            tar -xf "/tmp/${PREBUILT_NAME}" -C .deps/flac-prebuilt --strip-components=1
            cp -R .deps/flac-prebuilt/lib/. "${DEPS_PREFIX}/lib/"
            cp -R .deps/flac-prebuilt/include/. "${DEPS_PREFIX}/include/"
            echo "Using cached prebuilt libFLAC ${FLAC_VERSION} for ${FLAC_ARCH}"
          else
            echo "No prebuilt libFLAC for ${FLAC_ARCH}; building from cached source tarball."
            curl -fsSL -o "/tmp/${SRC_NAME}" "${CACHE_BASE}/${SRC_NAME}"
            echo "${FLAC_SRC_SHA256}  /tmp/${SRC_NAME}" | sha256sum -c -
            rm -rf .deps/flac-src .deps/flac
            mkdir -p .deps/flac-src
            tar -xf "/tmp/${SRC_NAME}" -C .deps/flac-src --strip-components=1
            cmake -S .deps/flac-src -B .deps/flac/build \
              -DCMAKE_BUILD_TYPE=Release \
              -DBUILD_SHARED_LIBS=OFF \
              -DWITH_OGG=OFF \
              -DBUILD_TESTING=OFF \
              -DCMAKE_INSTALL_PREFIX="${DEPS_PREFIX}"
            cmake --build .deps/flac/build --parallel
            cmake --install .deps/flac/build
            echo "Built libFLAC ${FLAC_VERSION} from cached source for ${FLAC_ARCH}"
          fi

          # Sanity: vendored flac.pc must report 1.5.0 before meson sees it.
          # meson.build hard-fails configure if this ever regresses below 1.5.0.
          PKG_CONFIG_PATH="${DEPS_PREFIX}/lib/pkgconfig:${DEPS_PREFIX}/lib64/pkgconfig" \
            pkg-config --modversion flac

          rm -rf .deps/hsdaoh
          cp -R third_party/hsdaoh .deps/hsdaoh
          git clone --depth 1 --branch 5.5 https://github.com/raysan5/raylib.git .deps/raylib
          # hsdaoh's CMakeLists uses pkg_check_modules(flac), so the vendored
          # flac.pc in DEPS_PREFIX must be on PKG_CONFIG_PATH now -- the apt
          # libflac-dev that used to provide a system flac.pc is no longer
          # installed. This export also benefits raylib/hsdaoh CMake finders.
          export PKG_CONFIG_PATH="${DEPS_PREFIX}/lib/pkgconfig:${DEPS_PREFIX}/lib64/pkgconfig:${PKG_CONFIG_PATH:-}"
          export CMAKE_PREFIX_PATH="${DEPS_PREFIX}:${CMAKE_PREFIX_PATH:-}"
          # hsdaoh's hsdaoh_file tool target includes "FLAC/metadata.h" but does
          # not link the flac pkg-config module, so it does not inherit flac's
          # -I include dir from CMake. With the old apt libflac-dev the headers
          # lived in /usr/include/FLAC (default system include path) so the
          # missing -I was masked; the vendored headers in DEPS_PREFIX/include/FLAC
          # are not on a default path. Export CFLAGS/CXXFLAGS with -I so every
          # hsdaoh target (including hsdaoh_file) can resolve FLAC/*.h.
          export CFLAGS="-I${DEPS_PREFIX}/include ${CFLAGS:-}"
          export CXXFLAGS="-I${DEPS_PREFIX}/include ${CXXFLAGS:-}"
          cmake -S .deps/hsdaoh -B .deps/hsdaoh/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_SHARED_LIBS=OFF \
            -DCMAKE_INSTALL_PREFIX="$DEPS_PREFIX" \
            -DINSTALL_UDEV_RULES=OFF
          cmake --build .deps/hsdaoh/build --parallel
          cmake --install .deps/hsdaoh/build
          for PCDIR in "$DEPS_PREFIX/lib/pkgconfig" "$DEPS_PREFIX/lib64/pkgconfig"; do
            if [[ -f "$PCDIR/libhsdaoh.pc" && ! -f "$PCDIR/hsdaoh.pc" ]]; then
              cp "$PCDIR/libhsdaoh.pc" "$PCDIR/hsdaoh.pc"
            fi
          done

          cmake -S .deps/raylib -B .deps/raylib/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_EXAMPLES=OFF \
            -DBUILD_GAMES=OFF \
            -DCMAKE_INSTALL_PREFIX="$DEPS_PREFIX"
          cmake --build .deps/raylib/build --parallel
          cmake --install .deps/raylib/build

      - name: Build MISRC tools (Meson)
        env:
          MISRC_TOOLS_VERSION_OVERRIDE: ${{ steps.version.outputs.version }}
        run: |
          set -eux
          DEPS_PREFIX="$PWD/.deps/install"
          export PKG_CONFIG_PATH="$DEPS_PREFIX/lib/pkgconfig:$DEPS_PREFIX/lib64/pkgconfig:${PKG_CONFIG_PATH:-}"
          export CMAKE_PREFIX_PATH="$DEPS_PREFIX:${CMAKE_PREFIX_PATH:-}"
          pkg-config --modversion fftw3f
          meson setup "$BUILD_DIR" misrc_tools --buildtype release
          meson compile -C "$BUILD_DIR" misrc_capture misrc_extract misrc_gui
          test -x "$BUILD_DIR/misrc_gui"

      - name: Smoke test built binaries
        run: |
          set -eux
          "$BUILD_DIR/misrc_gui" --smoke-test
          set +e
          "$BUILD_DIR/misrc_capture" -h > /tmp/misrc_capture_help.txt 2>&1
          RC_CAPTURE=$?
          "$BUILD_DIR/misrc_extract" -h > /tmp/misrc_extract_help.txt 2>&1
          RC_EXTRACT=$?
          set -e
          test "$RC_CAPTURE" -eq 1
          test "$RC_EXTRACT" -eq 1
          grep -qi "Usage" /tmp/misrc_capture_help.txt
          grep -qi "Usage" /tmp/misrc_extract_help.txt

      - name: Post-build CI guard tests (vendored deps + FX3 enabled)
        run: |
          set -eux
          python3 misrc_tools/test/ci_guard_tests.py --post-build --gui-path "$BUILD_DIR/misrc_gui"

      - name: Package AppImage artifact
        env:
          BUILD_VERSION: ${{ steps.version.outputs.version }}
          LINUXDEPLOY_ARCH: ${{ matrix.linuxdeploy_arch }}
        run: |
          set -eux
          mkdir -p AppDir/usr/bin
          cp "$BUILD_DIR/misrc_gui" AppDir/usr/bin/
          cp "$BUILD_DIR/misrc_capture" AppDir/usr/bin/
          cp "$BUILD_DIR/misrc_extract" AppDir/usr/bin/

          cat > AppDir/AppRun <<'EOF'
          #!/usr/bin/env bash
          HERE="$(dirname "$(readlink -f "$0")")"
          install_shortcuts() {
            local appimage_path="${APPIMAGE:-}"
            if [[ -z "$appimage_path" || ! -f "$appimage_path" ]]; then
              return 0
            fi
            local data_home="${XDG_DATA_HOME:-$HOME/.local/share}"
            local apps_dir="$data_home/applications"
            local icons_dir="$data_home/icons/hicolor/512x512/apps"
            local launcher="$apps_dir/misrc_gui.desktop"
            local local_bin_dir="$HOME/.local/bin"
            local stable_appimage="$local_bin_dir/misrc_gui.AppImage"
            local launcher_exec_path="$appimage_path"

            mkdir -p "$apps_dir" "$icons_dir" "$local_bin_dir"
            if ln -sfn "$appimage_path" "$stable_appimage"; then
              launcher_exec_path="$stable_appimage"
              chmod +x "$stable_appimage" || true
            fi
            local escaped_launcher_exec_path="$launcher_exec_path"
            escaped_launcher_exec_path="${escaped_launcher_exec_path//\\/\\\\}"
            escaped_launcher_exec_path="${escaped_launcher_exec_path//\"/\\\"}"
            local gui_version=""
            local startup_wm_class="misrc_gui"
            if [[ -x "$HERE/usr/bin/misrc_gui" ]]; then
              gui_version="$("$HERE/usr/bin/misrc_gui" --version 2>/dev/null | head -n1 | tr -d '\r')"
              if [[ -n "$gui_version" ]]; then
                startup_wm_class="MISRC Capture $gui_version"
              fi
            fi
            local escaped_startup_wm_class="$startup_wm_class"
            escaped_startup_wm_class="${escaped_startup_wm_class//\\/\\\\}"
            escaped_startup_wm_class="${escaped_startup_wm_class//\"/\\\"}"
            if [[ -f "$HERE/misrc.png" ]]; then
              cp -f "$HERE/misrc.png" "$icons_dir/misrc.png" || true
            fi
            {
              printf '%s\n' \
                "[Desktop Entry]" \
                "Type=Application" \
                "Name=MISRC GUI" \
                "Exec=\"${escaped_launcher_exec_path}\" %U" \
                "Icon=misrc" \
                "Categories=Utility;" \
                "StartupWMClass=${escaped_startup_wm_class}" \
                "X-GNOME-WMClass=${escaped_startup_wm_class}" \
                "Terminal=false" \
                "StartupNotify=true"
            } > "$launcher"
            chmod +x "$launcher" || true

            local desktop_dir=""
            if command -v xdg-user-dir >/dev/null 2>&1; then
              desktop_dir="$(xdg-user-dir DESKTOP 2>/dev/null || true)"
            fi
            if [[ -z "$desktop_dir" ]]; then
              desktop_dir="$HOME/Desktop"
            fi
            if [[ -d "$desktop_dir" ]]; then
              cp -f "$launcher" "$desktop_dir/MISRC GUI.desktop" || true
              chmod +x "$desktop_dir/MISRC GUI.desktop" || true
            fi
          }
          if [[ "${1:-}" == "--create-shortcut" ]]; then
            install_shortcuts
            exit 0
          fi
          if [[ $# -eq 0 ]]; then
            install_shortcuts
            exec "$HERE/usr/bin/misrc_gui"
          fi
          case "$1" in
            capture)
              shift
              exec "$HERE/usr/bin/misrc_capture" "$@"
              ;;
            extract)
              shift
              exec "$HERE/usr/bin/misrc_extract" "$@"
              ;;
            *)
              if [[ "${1:-}" != "--smoke-test" ]]; then
                install_shortcuts
              fi
              exec "$HERE/usr/bin/misrc_gui" "$@"
              ;;
          esac
          EOF
          chmod +x AppDir/AppRun

          cat > AppDir/misrc.desktop <<EOF
          [Desktop Entry]
          Type=Application
          Name=MISRC GUI
          Exec=misrc_gui
          Icon=misrc
          Categories=Utility;
          StartupWMClass=MISRC Capture ${BUILD_VERSION}
          X-GNOME-WMClass=MISRC Capture ${BUILD_VERSION}
          Terminal=false
          StartupNotify=true
          EOF
          ICON_SRC="assets/Icons/MISRC_Icon.png"
          test -f "$ICON_SRC"
          convert "$ICON_SRC" -resize 512x512\! AppDir/misrc.png
          ln -sf misrc.png AppDir/.DirIcon
          mkdir -p AppDir/usr/share/icons/hicolor/512x512/apps
          cp AppDir/misrc.png AppDir/usr/share/icons/hicolor/512x512/apps/misrc.png

          curl -L -o linuxdeploy "https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-${LINUXDEPLOY_ARCH}.AppImage"
          curl -L -o linuxdeploy-plugin-appimage "https://github.com/linuxdeploy/linuxdeploy-plugin-appimage/releases/download/continuous/linuxdeploy-plugin-appimage-${LINUXDEPLOY_ARCH}.AppImage"
          chmod +x linuxdeploy linuxdeploy-plugin-appimage

          ARCH="$LINUXDEPLOY_ARCH" APPIMAGE_EXTRACT_AND_RUN=1 ./linuxdeploy \
            --appdir AppDir \
            -e AppDir/usr/bin/misrc_gui \
            -d AppDir/misrc.desktop \
            -i AppDir/misrc.png \
            --output appimage

          APPIMAGE_NAME="Linux_MISRC_GUI_${BUILD_VERSION}_${{ matrix.artifact_suffix }}.AppImage"
          mv ./*.AppImage "$APPIMAGE_NAME"
          test -f "$APPIMAGE_NAME"

          APPIMAGE_EXTRACT_AND_RUN=1 "./$APPIMAGE_NAME" --smoke-test
          set +e
          APPIMAGE_EXTRACT_AND_RUN=1 "./$APPIMAGE_NAME" capture -h > /tmp/appimage_capture_help.txt 2>&1
          RC_CAPTURE=$?
          APPIMAGE_EXTRACT_AND_RUN=1 "./$APPIMAGE_NAME" extract -h > /tmp/appimage_extract_help.txt 2>&1
          RC_EXTRACT=$?
          set -e
          test "$RC_CAPTURE" -eq 1
          test "$RC_EXTRACT" -eq 1
          grep -qi "Usage" /tmp/appimage_capture_help.txt
          grep -qi "Usage" /tmp/appimage_extract_help.txt
          ZIP_NAME="Linux_MISRC_GUI_${BUILD_VERSION}_${{ matrix.artifact_suffix }}.zip"
          zip -j "$ZIP_NAME" "$APPIMAGE_NAME"
          test -f "$ZIP_NAME"
          test "$(unzip -Z1 "$ZIP_NAME" | wc -l)" -eq 1
          test "$(unzip -Z1 "$ZIP_NAME")" = "$APPIMAGE_NAME"
      - name: Upload Linux ZIP
        uses: actions/upload-artifact@v7
        with:
          name: linux-zip-${{ matrix.arch }}
          path: Linux_MISRC_GUI_*_${{ matrix.artifact_suffix }}.zip
          if-no-files-found: error

  android-apk:
    name: Android APK build (arm64-v8a)
    runs-on: ubuntu-22.04
    needs:
      - preflight-guard-tests
      - preflight-cross-platform-guards
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Resolve build version
        id: version
        env:
          GITHUB_REF: ${{ github.ref }}
          CI_EVENT_NAME: ${{ github.event_name }}
          CI_CREATE_RELEASE: ${{ github.event.inputs.create_release }}
          CI_RELEASE_TAG: ${{ github.event.inputs.release_tag }}
        run: |
          set -eux
          VERSION="$(misrc_tools/ci-resolve-version.sh)"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Android SDK + NDK
        run: |
          set -eux
          export ANDROID_HOME="$HOME/Android/Sdk"
          mkdir -p "$ANDROID_HOME/cmdline-tools"
          curl -fL -o /tmp/cmdtools.zip https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          unzip -q /tmp/cmdtools.zip -d /tmp/android-cmdtools
          mv /tmp/android-cmdtools/cmdline-tools "$ANDROID_HOME/cmdline-tools/latest"
          rm /tmp/cmdtools.zip
          export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH"
          yes 2>/dev/null | sdkmanager --licenses >/dev/null 2>&1 || true
          yes 2>/dev/null | sdkmanager "platform-tools" "build-tools;34.0.0" "platforms;android-30" "platforms;android-34" "ndk;25.2.9519653" >/dev/null
          echo "ANDROID_HOME=$ANDROID_HOME" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$ANDROID_HOME" >> "$GITHUB_ENV"
          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/25.2.9519653" >> "$GITHUB_ENV"
          echo "NDK_HOME=$ANDROID_HOME/ndk/25.2.9519653" >> "$GITHUB_ENV"
          echo "$ANDROID_HOME/cmdline-tools/latest/bin" >> "$GITHUB_PATH"
          echo "$ANDROID_HOME/platform-tools" >> "$GITHUB_PATH"

      - name: Install build dependencies
        run: |
          set -eux
          sudo apt-get update
          sudo apt-get install -y cmake meson ninja-build nasm pkg-config curl zip unzip

      - name: Cross-build Android deps
        run: |
          set -eux
          export DEPS_PREFIX="$PWD/.deps/install-android-arm64"
          bash android/build-deps-android.sh

      - name: Generate Meson cross-file
        run: |
          set -eux
          export DEPS_PREFIX="$PWD/.deps/install-android-arm64"
          bash android/gen-cross-file.sh

      - name: Cross-compile misrc_gui (libmisrc_gui.so)
        run: |
          set -eux
          export DEPS_PREFIX="$PWD/.deps/install-android-arm64"
          export PKG_CONFIG="$PWD/android/android-pkg-config"
          meson setup --cross-file android/aarch64-linux-android.ini -Dbuildtype=release build-android misrc_tools
          meson compile -C build-android misrc_gui
          test -f build-android/libmisrc_gui.so

      - name: Build + sign APK
        env:
          MISRC_TOOLS_VERSION_OVERRIDE: ${{ steps.version.outputs.version }}
        run: |
          set -eux
          export DEPS_PREFIX="$PWD/.deps/install-android-arm64"
          export BUILD_DIR="$PWD/build-android"
          bash android/build-apk.sh

      - name: Verify APK
        run: |
          set -eux
          APK=$(ls -t .ci-artifacts/android-apk/*.apk | head -1)
          test -f "$APK"
          "$ANDROID_HOME/build-tools/34.0.0/apksigner" verify "$APK"
          BADGING="$("$ANDROID_HOME/build-tools/34.0.0/aapt2" dump badging "$APK")"
          echo "$BADGING" | grep -q "sdkVersion:'30'"
          echo "$BADGING" | grep -q "targetSdkVersion:'34'"
          echo "$BADGING" | grep -q "package: name='dev.misrc.gui'"
          unzip -l "$APK" | grep -q "lib/arm64-v8a/libmisrc_gui.so"
          unzip -p "$APK" lib/arm64-v8a/libmisrc_gui.so | file - | grep -q "ARM aarch64"
          NDK_TOOLCHAIN="$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin"
          "${NDK_TOOLCHAIN}/llvm-readelf" -d build-android/libmisrc_gui.so | grep NEEDED | grep -iE "/usr/lib/x86_64|/usr/local/lib" && {
            echo "ERROR: host x86_64 lib leaked into arm64 cross build"; exit 1; } || true

      - name: Upload Android APK
        uses: actions/upload-artifact@v7
        with:
          name: android-apk-arm64
          path: .ci-artifacts/android-apk/*.apk
          if-no-files-found: error

  windows-exe:
    name: Windows EXE build (x86_64)
    runs-on: windows-2022
    needs:
      - preflight-guard-tests
      - preflight-cross-platform-guards
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Cache vendored deps (.deps/install)
        id: deps-cache
        uses: actions/cache@v6
        with:
          path: .deps/install
          key: deps-windows-x86_64-raylib5.5-libuvc-v0.0.7-${{ hashFiles('third_party/hsdaoh/**') }}

      - name: Setup MSYS2 (cached)
        id: setup_msys2_cached
        continue-on-error: true
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          cache: true
          install: >-
            git
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-fftw
            mingw-w64-x86_64-flac
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-libjpeg-turbo
            mingw-w64-x86_64-libusb
            mingw-w64-x86_64-libsoxr
            mingw-w64-x86_64-meson
            mingw-w64-x86_64-nasm
            mingw-w64-x86_64-ninja
            mingw-w64-x86_64-pkgconf
            mingw-w64-x86_64-rtl-sdr

      - name: Setup MSYS2 (retry no cache)
        if: steps.setup_msys2_cached.outcome == 'failure'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          cache: false
          install: >-
            git
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-fftw
            mingw-w64-x86_64-flac
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-libjpeg-turbo
            mingw-w64-x86_64-libusb
            mingw-w64-x86_64-libsoxr
            mingw-w64-x86_64-meson
            mingw-w64-x86_64-nasm
            mingw-w64-x86_64-ninja
            mingw-w64-x86_64-pkgconf
            mingw-w64-x86_64-rtl-sdr

      - name: Resolve build version
        id: version
        env:
          GITHUB_REF: ${{ github.ref }}
          CI_EVENT_NAME: ${{ github.event_name }}
          CI_CREATE_RELEASE: ${{ github.event.inputs.create_release }}
          CI_RELEASE_TAG: ${{ github.event.inputs.release_tag }}
        shell: msys2 {0}
        run: |
          set -euo pipefail
          VERSION="$(misrc_tools/ci-resolve-version.sh)"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Build and package EXE
        shell: msys2 {0}
        env:
          MISRC_TOOLS_VERSION_OVERRIDE: ${{ steps.version.outputs.version }}
        run: |
          set -euo pipefail
          DEPS_PREFIX="$PWD/.deps/install"
          export PKG_CONFIG_PATH="$DEPS_PREFIX/lib/pkgconfig:$DEPS_PREFIX/lib64/pkgconfig:${MINGW_PREFIX}/lib/pkgconfig:${PKG_CONFIG_PATH:-}"

          # Cache hit guard: skip libuvc/hsdaoh/raylib rebuild if .deps/install
          # was restored from actions/cache (key includes hashFiles(hsdaoh/**)).
          if [[ ! -f "$DEPS_PREFIX/lib/pkgconfig/hsdaoh.pc" ]]; then
          git clone --depth 1 --branch v0.0.7 https://github.com/libuvc/libuvc.git .deps/libuvc
          sed -i 's/^if (MSVC OR MINGW)$/if (MSVC)/' .deps/libuvc/cmake/FindLibUSB.cmake
          cmake -S .deps/libuvc -B .deps/libuvc/build -G Ninja \
            -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_BUILD_TARGET=Static \
            -DBUILD_SHARED_LIBS=OFF \
            -DBUILD_EXAMPLE=OFF \
            -DBUILD_TEST=OFF \
            -DCMAKE_INSTALL_PREFIX="$DEPS_PREFIX"
          cmake --build .deps/libuvc/build --parallel
          cmake --install .deps/libuvc/build
          mkdir -p .deps

          rm -rf .deps/hsdaoh
          cp -R third_party/hsdaoh .deps/hsdaoh
          git clone --depth 1 --branch 5.5 https://github.com/raysan5/raylib.git .deps/raylib
          cmake -S .deps/hsdaoh -B .deps/hsdaoh/build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_SHARED_LIBS=OFF \
            -DCMAKE_INSTALL_PREFIX="$DEPS_PREFIX" \
            -DINSTALL_UDEV_RULES=OFF
          cmake --build .deps/hsdaoh/build --target hsdaoh_static --parallel
          mkdir -p "$DEPS_PREFIX/include" "$DEPS_PREFIX/lib" "$DEPS_PREFIX/lib/pkgconfig"
          for hdr in hsdaoh.h hsdaoh_export.h hsdaoh_raw.h hsdaoh_crc.h; do
            if [[ -f ".deps/hsdaoh/include/$hdr" ]]; then
              cp ".deps/hsdaoh/include/$hdr" "$DEPS_PREFIX/include/"
            fi
          done
          if [[ -f .deps/hsdaoh/build/src/libhsdaoh.a ]]; then
            cp .deps/hsdaoh/build/src/libhsdaoh.a "$DEPS_PREFIX/lib/libhsdaoh.a"
          elif [[ -f .deps/hsdaoh/build/src/libhsdaoh_static.a ]]; then
            cp .deps/hsdaoh/build/src/libhsdaoh_static.a "$DEPS_PREFIX/lib/libhsdaoh.a"
          elif [[ -f .deps/hsdaoh/build/src/hsdaoh_static.lib ]]; then
            cp .deps/hsdaoh/build/src/hsdaoh_static.lib "$DEPS_PREFIX/lib/libhsdaoh.a"
          else
            echo "hsdaoh static library not found in expected build output paths" >&2
            exit 1
          fi
          cp .deps/hsdaoh/build/libhsdaoh.pc "$DEPS_PREFIX/lib/pkgconfig/libhsdaoh.pc"
          sed -i 's/^Libs: .*/Libs: -L${libdir} -lhsdaoh -luvc -lusb-1.0/' "$DEPS_PREFIX/lib/pkgconfig/libhsdaoh.pc"
          for PCDIR in "$DEPS_PREFIX/lib/pkgconfig" "$DEPS_PREFIX/lib64/pkgconfig"; do
            if [[ -f "$PCDIR/libhsdaoh.pc" && ! -f "$PCDIR/hsdaoh.pc" ]]; then
              cp "$PCDIR/libhsdaoh.pc" "$PCDIR/hsdaoh.pc"
            fi
          done
          cmake -S .deps/raylib -B .deps/raylib/build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_EXAMPLES=OFF \
            -DBUILD_GAMES=OFF \
            -DCMAKE_INSTALL_PREFIX="$DEPS_PREFIX"
          cmake --build .deps/raylib/build --parallel
          cmake --install .deps/raylib/build
          else
            echo "deps cache hit - skipping libuvc/hsdaoh/raylib rebuild"
          fi

          export PKG_CONFIG_PATH="$DEPS_PREFIX/lib/pkgconfig:$DEPS_PREFIX/lib64/pkgconfig:${PKG_CONFIG_PATH:-}"
          export CMAKE_PREFIX_PATH="$DEPS_PREFIX:${CMAKE_PREFIX_PATH:-}"
          pkg-config --modversion fftw3f
          pkg-config --modversion librtlsdr soxr
          meson setup "$BUILD_DIR" misrc_tools --buildtype release
          meson compile -C "$BUILD_DIR" misrc_capture misrc_extract misrc_gui

          mkdir -p dist
          cp "$BUILD_DIR/misrc_gui.exe" dist/MISRC.exe
          test "$(find dist -maxdepth 1 -type f | wc -l)" -eq 1
          test "$(find dist -maxdepth 1 -name '*.exe' | wc -l)" -eq 1
          test "$(objdump -p dist/MISRC.exe | awk '/^Subsystem[[:space:]]/ {print $2; exit}')" = "00000002"
          assert_no_nonsystem_dlls() {
            local target="$1"
            while IFS= read -r dll; do
              case "$(echo "$dll" | tr '[:lower:]' '[:upper:]')" in
                API-MS-WIN-*|KERNEL32.DLL|KERNELBASE.DLL|USER32.DLL|GDI32.DLL|SHELL32.DLL|WINMM.DLL|OLE32.DLL|OLEAUT32.DLL|ADVAPI32.DLL|COMDLG32.DLL|COMCTL32.DLL|SHLWAPI.DLL|WS2_32.DLL|SETUPAPI.DLL|IMM32.DLL|VERSION.DLL|UCRTBASE.DLL|MSVCRT.DLL|MF.DLL|MFPLAT.DLL|MFREADWRITE.DLL|EVR.DLL|STRMIIDS.DLL|AVRT.DLL)
                  continue
                  ;;
                *)
                  echo "Non-system runtime DLL dependency detected in $target: $dll" >&2
                  exit 1
                  ;;
              esac
            done < <(objdump -p "$target" | awk '/DLL Name:/ {print $3}')
          }

          assert_no_nonsystem_dlls "dist/MISRC.exe"
          ./dist/MISRC.exe --smoke-test
          set +e
          "$BUILD_DIR/misrc_capture.exe" -h > capture_help.txt 2>&1
          RC_CAPTURE=$?
          "$BUILD_DIR/misrc_extract.exe" -h > extract_help.txt 2>&1
          RC_EXTRACT=$?
          set -e
          test "$RC_CAPTURE" -eq 1
          test "$RC_EXTRACT" -eq 1
          grep -qi "Usage" capture_help.txt
          grep -qi "Usage" extract_help.txt

          # Post-build CI guard tests: assert no stale hsdaoh DLL dependency
          # (Windows statically links the vendored .deps/install libhsdaoh.a)
          # and that FX3/RTL-SDR compiled in. Uses MSYS2 python + objdump.
          python misrc_tools/test/ci_guard_tests.py --post-build --require-rtlsdr --gui-path "$BUILD_DIR/misrc_gui.exe"

      - name: Create ZIP artifact
        shell: pwsh
        run: |
          $ZipPath = "Windows_MISRC_GUI_${{ steps.version.outputs.version }}_x86.zip"
          Compress-Archive -Path @("dist/MISRC.exe") -DestinationPath $ZipPath -Force
          Add-Type -AssemblyName System.IO.Compression.FileSystem
          $zip = [System.IO.Compression.ZipFile]::OpenRead((Resolve-Path $ZipPath))
          try {
            if ($zip.Entries.Count -ne 1) {
              throw "Expected 1 entry in $ZipPath, found $($zip.Entries.Count)"
            }
            foreach ($entry in $zip.Entries) {
              if ($entry.FullName.Contains('/') -or $entry.FullName.Contains('\')) {
                throw "ZIP entry is nested; expected flat layout: $($entry.FullName)"
              }
            }
          } finally {
            $zip.Dispose()
          }

      - name: Upload Windows ZIP
        uses: actions/upload-artifact@v7
        with:
          name: windows-exe
          path: Windows_MISRC_GUI_*_x86.zip
          if-no-files-found: error
  windows-exe-arm64:
    name: Windows EXE build (arm64)
    runs-on: windows-11-arm
    needs:
      - preflight-guard-tests
      - preflight-cross-platform-guards
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Cache vendored deps (.deps/install)
        id: deps-cache
        uses: actions/cache@v6
        with:
          path: .deps/install
          key: deps-windows-arm64-raylib5.5-libuvc-v0.0.7-${{ hashFiles('third_party/hsdaoh/**') }}

      - name: Setup MSYS2 (cached)
        id: setup_msys2_cached
        continue-on-error: true
        uses: msys2/setup-msys2@v2
        with:
          msystem: CLANGARM64
          update: true
          cache: true
          install: >-
            git
            mingw-w64-clang-aarch64-binutils
            mingw-w64-clang-aarch64-clang
            mingw-w64-clang-aarch64-cmake
            mingw-w64-clang-aarch64-fftw
            mingw-w64-clang-aarch64-flac
            mingw-w64-clang-aarch64-libjpeg-turbo
            mingw-w64-clang-aarch64-libusb
            mingw-w64-clang-aarch64-meson
            mingw-w64-clang-aarch64-ninja
            mingw-w64-clang-aarch64-pkgconf
            mingw-w64-clang-aarch64-rtl-sdr

      - name: Setup MSYS2 (retry no cache)
        if: steps.setup_msys2_cached.outcome == 'failure'
        uses: msys2/setup-msys2@v2
        with:
          msystem: CLANGARM64
          update: true
          cache: false
          install: >-
            git
            mingw-w64-clang-aarch64-binutils
            mingw-w64-clang-aarch64-clang
            mingw-w64-clang-aarch64-cmake
            mingw-w64-clang-aarch64-fftw
            mingw-w64-clang-aarch64-flac
            mingw-w64-clang-aarch64-libjpeg-turbo
            mingw-w64-clang-aarch64-libusb
            mingw-w64-clang-aarch64-meson
            mingw-w64-clang-aarch64-ninja
            mingw-w64-clang-aarch64-pkgconf
            mingw-w64-clang-aarch64-rtl-sdr

      - name: Resolve build version
        id: version
        env:
          GITHUB_REF: ${{ github.ref }}
          CI_EVENT_NAME: ${{ github.event_name }}
          CI_CREATE_RELEASE: ${{ github.event.inputs.create_release }}
          CI_RELEASE_TAG: ${{ github.event.inputs.release_tag }}
        shell: msys2 {0}
        run: |
          set -euo pipefail
          VERSION="$(misrc_tools/ci-resolve-version.sh)"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Build and package EXE (ARM64)
        shell: msys2 {0}
        env:
          MISRC_TOOLS_VERSION_OVERRIDE: ${{ steps.version.outputs.version }}
        run: |
          set -euo pipefail
          DEPS_PREFIX="$PWD/.deps/install"
          export PKG_CONFIG_PATH="$DEPS_PREFIX/lib/pkgconfig:$DEPS_PREFIX/lib64/pkgconfig:${MINGW_PREFIX}/lib/pkgconfig:${PKG_CONFIG_PATH:-}"

          # Cache hit guard: skip libuvc/hsdaoh/raylib rebuild if .deps/install
          # was restored from actions/cache (key includes hashFiles(hsdaoh/**)).
          if [[ ! -f "$DEPS_PREFIX/lib/pkgconfig/hsdaoh.pc" ]]; then
          git clone --depth 1 --branch v0.0.7 https://github.com/libuvc/libuvc.git .deps/libuvc
          sed -i 's/^if (MSVC OR MINGW)$/if (MSVC)/' .deps/libuvc/cmake/FindLibUSB.cmake
          cmake -S .deps/libuvc -B .deps/libuvc/build -G Ninja \
            -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_BUILD_TARGET=Static \
            -DBUILD_SHARED_LIBS=OFF \
            -DBUILD_EXAMPLE=OFF \
            -DBUILD_TEST=OFF \
            -DCMAKE_INSTALL_PREFIX="$DEPS_PREFIX"
          cmake --build .deps/libuvc/build --parallel
          cmake --install .deps/libuvc/build
          mkdir -p .deps

          rm -rf .deps/hsdaoh
          cp -R third_party/hsdaoh .deps/hsdaoh
          git clone --depth 1 --branch 5.5 https://github.com/raysan5/raylib.git .deps/raylib
          cmake -S .deps/hsdaoh -B .deps/hsdaoh/build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_SHARED_LIBS=OFF \
            -DCMAKE_INSTALL_PREFIX="$DEPS_PREFIX" \
            -DINSTALL_UDEV_RULES=OFF
          cmake --build .deps/hsdaoh/build --target hsdaoh_static --parallel
          mkdir -p "$DEPS_PREFIX/include" "$DEPS_PREFIX/lib" "$DEPS_PREFIX/lib/pkgconfig"
          for hdr in hsdaoh.h hsdaoh_export.h hsdaoh_raw.h hsdaoh_crc.h; do
            if [[ -f ".deps/hsdaoh/include/$hdr" ]]; then
              cp ".deps/hsdaoh/include/$hdr" "$DEPS_PREFIX/include/"
            fi
          done
          if [[ -f .deps/hsdaoh/build/src/libhsdaoh.a ]]; then
            cp .deps/hsdaoh/build/src/libhsdaoh.a "$DEPS_PREFIX/lib/libhsdaoh.a"
          elif [[ -f .deps/hsdaoh/build/src/libhsdaoh_static.a ]]; then
            cp .deps/hsdaoh/build/src/libhsdaoh_static.a "$DEPS_PREFIX/lib/libhsdaoh.a"
          elif [[ -f .deps/hsdaoh/build/src/hsdaoh_static.lib ]]; then
            cp .deps/hsdaoh/build/src/hsdaoh_static.lib "$DEPS_PREFIX/lib/libhsdaoh.a"
          else
            echo "hsdaoh static library not found in expected build output paths" >&2
            exit 1
          fi
          cp .deps/hsdaoh/build/libhsdaoh.pc "$DEPS_PREFIX/lib/pkgconfig/libhsdaoh.pc"
          sed -i 's/^Libs: .*/Libs: -L${libdir} -lhsdaoh -luvc -lusb-1.0/' "$DEPS_PREFIX/lib/pkgconfig/libhsdaoh.pc"
          for PCDIR in "$DEPS_PREFIX/lib/pkgconfig" "$DEPS_PREFIX/lib64/pkgconfig"; do
            if [[ -f "$PCDIR/libhsdaoh.pc" && ! -f "$PCDIR/hsdaoh.pc" ]]; then
              cp "$PCDIR/libhsdaoh.pc" "$PCDIR/hsdaoh.pc"
            fi
          done
          cmake -S .deps/raylib -B .deps/raylib/build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_EXAMPLES=OFF \
            -DBUILD_GAMES=OFF \
            -DCMAKE_INSTALL_PREFIX="$DEPS_PREFIX"
          cmake --build .deps/raylib/build --parallel
          cmake --install .deps/raylib/build
          else
            echo "deps cache hit - skipping libuvc/hsdaoh/raylib rebuild"
          fi

          export PKG_CONFIG_PATH="$DEPS_PREFIX/lib/pkgconfig:$DEPS_PREFIX/lib64/pkgconfig:${PKG_CONFIG_PATH:-}"
          export CMAKE_PREFIX_PATH="$DEPS_PREFIX:${CMAKE_PREFIX_PATH:-}"
          pkg-config --modversion fftw3f
          pkg-config --modversion librtlsdr
          meson setup "$BUILD_DIR" misrc_tools --buildtype release
          meson compile -C "$BUILD_DIR" misrc_capture misrc_extract misrc_gui

          mkdir -p dist
          cp "$BUILD_DIR/misrc_gui.exe" dist/MISRC.exe
          test "$(find dist -maxdepth 1 -type f | wc -l)" -eq 1
          test "$(find dist -maxdepth 1 -name '*.exe' | wc -l)" -eq 1
          test "$(objdump -p dist/MISRC.exe | awk '/^Subsystem[[:space:]]/ {print $2; exit}')" = "00000002"
          objdump -f dist/MISRC.exe | grep -Ei 'architecture:[[:space:]]*(aarch64|arm64)'
          assert_no_nonsystem_dlls() {
            local target="$1"
            while IFS= read -r dll; do
              case "$(echo "$dll" | tr '[:lower:]' '[:upper:]')" in
                API-MS-WIN-*|KERNEL32.DLL|KERNELBASE.DLL|USER32.DLL|GDI32.DLL|SHELL32.DLL|WINMM.DLL|OLE32.DLL|OLEAUT32.DLL|ADVAPI32.DLL|COMDLG32.DLL|COMCTL32.DLL|SHLWAPI.DLL|WS2_32.DLL|SETUPAPI.DLL|IMM32.DLL|VERSION.DLL|UCRTBASE.DLL|MSVCRT.DLL|MF.DLL|MFPLAT.DLL|MFREADWRITE.DLL|EVR.DLL|STRMIIDS.DLL|AVRT.DLL)
                  continue
                  ;;
                *)
                  echo "Non-system runtime DLL dependency detected in $target: $dll" >&2
                  exit 1
                  ;;
              esac
            done < <(objdump -p "$target" | awk '/DLL Name:/ {print $3}')
          }

          assert_no_nonsystem_dlls "dist/MISRC.exe"
          # Windows ARM64 binaries cannot be executed on windows-2022 x64 runners.
          # Post-build CI guard tests still run (binary introspection only, no exec):
          # assert no stale hsdaoh DLL dependency (static link expected) + FX3/RTL-SDR.
          python misrc_tools/test/ci_guard_tests.py --post-build --require-rtlsdr --gui-path "$BUILD_DIR/misrc_gui.exe"

      - name: Create ZIP artifact
        shell: pwsh
        run: |
          $ZipPath = "Windows_MISRC_GUI_${{ steps.version.outputs.version }}_arm64.zip"
          Compress-Archive -Path @("dist/MISRC.exe") -DestinationPath $ZipPath -Force
          Add-Type -AssemblyName System.IO.Compression.FileSystem
          $zip = [System.IO.Compression.ZipFile]::OpenRead((Resolve-Path $ZipPath))
          try {
            if ($zip.Entries.Count -ne 1) {
              throw "Expected 1 entry in $ZipPath, found $($zip.Entries.Count)"
            }
            foreach ($entry in $zip.Entries) {
              if ($entry.FullName.Contains('/') -or $entry.FullName.Contains('\')) {
                throw "ZIP entry is nested; expected flat layout: $($entry.FullName)"
              }
            }
          } finally {
            $zip.Dispose()
          }

      - name: Upload Windows ARM64 ZIP
        uses: actions/upload-artifact@v7
        with:
          name: windows-exe-arm64
          path: Windows_MISRC_GUI_*_arm64.zip
          if-no-files-found: error

  macos-app-build:
    name: macOS APP build (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            runner: macos-14
          - arch: x86_64
            runner: macos-15-intel
    runs-on: ${{ matrix.runner }}
    needs:
      - preflight-guard-tests
      - preflight-cross-platform-guards
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Cache vendored deps (.deps/install)
        id: deps-cache
        uses: actions/cache@v6
        with:
          path: .deps/install
          key: deps-macos-${{ matrix.arch }}-raylib5.5-libuvc-v0.0.7-${{ hashFiles('third_party/hsdaoh/**') }}

      - name: Resolve build version
        id: version
        env:
          GITHUB_REF: ${{ github.ref }}
          CI_EVENT_NAME: ${{ github.event_name }}
          CI_CREATE_RELEASE: ${{ github.event.inputs.create_release }}
          CI_RELEASE_TAG: ${{ github.event.inputs.release_tag }}
        run: |
          set -eux
          VERSION="$(misrc_tools/ci-resolve-version.sh)"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Install build dependencies
        env:
          # GitHub-hosted macOS runners ship a recently-updated Homebrew, so
          # skip the slow `brew update` (which also triggers re-linking that
          # emits the 'files were overwritten during the brew link step'
          # annotation). Only install formulas that are not already present.
          HOMEBREW_NO_AUTO_UPDATE: '1'
          HOMEBREW_NO_INSTALL_CLEANUP: '1'
        run: |
          set -eux
          for tap_dir in \
            /opt/homebrew/Library/Taps/aws/homebrew-tap \
            /opt/homebrew/Library/Taps/azure/homebrew-bicep \
            /opt/homebrew/Library/Taps/hashicorp/homebrew-tap \
            /usr/local/Homebrew/Library/Taps/aws/homebrew-tap \
            /usr/local/Homebrew/Library/Taps/azure/homebrew-bicep \
            /usr/local/Homebrew/Library/Taps/hashicorp/homebrew-tap; do
            rm -rf "$tap_dir"
          done
          for formula in cmake fftw flac libusb libuvc meson nasm ninja pkgconf libsoxr librtlsdr; do
            if ! brew list --versions "$formula" >/dev/null 2>&1; then
              brew install "$formula"
            fi
          done

      - name: Build and stage architecture app bundle
        env:
          BUILD_VERSION: ${{ steps.version.outputs.version }}
          MACOS_ARCH: ${{ matrix.arch }}
          MISRC_TOOLS_VERSION_OVERRIDE: ${{ steps.version.outputs.version }}
        run: |
          set -eux
          DEPS_PREFIX="$PWD/.deps/install"
          FLAC_PREFIX="$(brew --prefix flac)"
          BREW_PREFIX="$(brew --prefix)"
          export CPPFLAGS="-I${BREW_PREFIX}/include ${CPPFLAGS:-}"
          export CFLAGS="-arch ${MACOS_ARCH} ${CFLAGS:-}"
          export CXXFLAGS="-arch ${MACOS_ARCH} ${CXXFLAGS:-}"
          export LDFLAGS="-arch ${MACOS_ARCH} ${LDFLAGS:-}"
          mkdir -p .deps

          # Cache hit guard: skip hsdaoh/raylib rebuild if .deps/install was
          # restored from actions/cache (key includes hashFiles(hsdaoh/**)).
          if [[ ! -f "$DEPS_PREFIX/lib/pkgconfig/hsdaoh.pc" ]]; then
          rm -rf .deps/hsdaoh
          cp -R third_party/hsdaoh .deps/hsdaoh
          git clone --depth 1 --branch 5.5 https://github.com/raysan5/raylib.git .deps/raylib
          cmake -S .deps/hsdaoh -B .deps/hsdaoh/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_FLAGS="-I${FLAC_PREFIX}/include -I${BREW_PREFIX}/include" \
            -DCMAKE_OSX_ARCHITECTURES="${MACOS_ARCH}" \
            -DBUILD_SHARED_LIBS=OFF \
            -DCMAKE_INSTALL_PREFIX="$DEPS_PREFIX" \
            -DINSTALL_UDEV_RULES=OFF
          cmake --build .deps/hsdaoh/build --parallel
          cmake --install .deps/hsdaoh/build
          for PCDIR in "$DEPS_PREFIX/lib/pkgconfig" "$DEPS_PREFIX/lib64/pkgconfig"; do
            if [[ -f "$PCDIR/libhsdaoh.pc" && ! -f "$PCDIR/hsdaoh.pc" ]]; then
              cp "$PCDIR/libhsdaoh.pc" "$PCDIR/hsdaoh.pc"
            fi
          done
          cmake -S .deps/raylib -B .deps/raylib/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES="${MACOS_ARCH}" \
            -DBUILD_EXAMPLES=OFF \
            -DBUILD_GAMES=OFF \
            -DCMAKE_INSTALL_PREFIX="$DEPS_PREFIX"
          cmake --build .deps/raylib/build --parallel
          cmake --install .deps/raylib/build
          else
            echo "deps cache hit - skipping hsdaoh/raylib rebuild"
          fi

          export PKG_CONFIG_PATH="$DEPS_PREFIX/lib/pkgconfig:$DEPS_PREFIX/lib64/pkgconfig:${PKG_CONFIG_PATH:-}"
          export CMAKE_PREFIX_PATH="$DEPS_PREFIX:${CMAKE_PREFIX_PATH:-}"
          pkg-config --modversion fftw3f
          pkg-config --modversion librtlsdr soxr
          meson setup "$BUILD_DIR" misrc_tools --buildtype release
          meson compile -C "$BUILD_DIR" misrc_capture misrc_extract misrc_gui

          "$BUILD_DIR/misrc_gui" --smoke-test
          set +e
          "$BUILD_DIR/misrc_capture" -h > /tmp/macos_capture_help.txt 2>&1
          RC_CAPTURE=$?
          "$BUILD_DIR/misrc_extract" -h > /tmp/macos_extract_help.txt 2>&1
          RC_EXTRACT=$?
          set -e
          test "$RC_CAPTURE" -eq 1
          test "$RC_EXTRACT" -eq 1
          grep -qi "Usage" /tmp/macos_capture_help.txt
          grep -qi "Usage" /tmp/macos_extract_help.txt

          # Post-build CI guard tests: assert no stale Homebrew hsdaoh linkage
          # (macOS must use the vendored .deps/install hsdaoh via @rpath) + FX3/RTL-SDR.
          python3 misrc_tools/test/ci_guard_tests.py --post-build --require-rtlsdr --gui-path "$BUILD_DIR/misrc_gui"

          APP_ROOT="dist/MISRC.app/Contents"
          FW_DIR="$APP_ROOT/Frameworks"
          mkdir -p "$APP_ROOT/MacOS" "$APP_ROOT/Resources" "$FW_DIR"
          ICON_SRC="assets/Icons/MISRC_Icon.png"
          test -f "$ICON_SRC"
          ICONSET_DIR="dist/MISRC.iconset"
          mkdir -p "$ICONSET_DIR"
          sips -z 16 16 "$ICON_SRC" --out "$ICONSET_DIR/icon_16x16.png"
          sips -z 32 32 "$ICON_SRC" --out "$ICONSET_DIR/icon_16x16@2x.png"
          sips -z 32 32 "$ICON_SRC" --out "$ICONSET_DIR/icon_32x32.png"
          sips -z 64 64 "$ICON_SRC" --out "$ICONSET_DIR/icon_32x32@2x.png"
          sips -z 128 128 "$ICON_SRC" --out "$ICONSET_DIR/icon_128x128.png"
          sips -z 256 256 "$ICON_SRC" --out "$ICONSET_DIR/icon_128x128@2x.png"
          sips -z 256 256 "$ICON_SRC" --out "$ICONSET_DIR/icon_256x256.png"
          sips -z 512 512 "$ICON_SRC" --out "$ICONSET_DIR/icon_256x256@2x.png"
          sips -z 512 512 "$ICON_SRC" --out "$ICONSET_DIR/icon_512x512.png"
          sips -z 1024 1024 "$ICON_SRC" --out "$ICONSET_DIR/icon_512x512@2x.png"
          iconutil -c icns "$ICONSET_DIR" -o "$APP_ROOT/Resources/MISRC.icns"
          rm -rf "$ICONSET_DIR"
          cp "$BUILD_DIR/misrc_gui" "$APP_ROOT/MacOS/MISRC"
          cp "$BUILD_DIR/misrc_capture" "$APP_ROOT/MacOS/misrc_capture"
          cp "$BUILD_DIR/misrc_extract" "$APP_ROOT/MacOS/misrc_extract"
          chmod +x "$APP_ROOT/MacOS/MISRC" "$APP_ROOT/MacOS/misrc_capture" "$APP_ROOT/MacOS/misrc_extract"

          cat > "$APP_ROOT/Info.plist" <<'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>CFBundleName</key>
            <string>MISRC</string>
            <key>CFBundleDisplayName</key>
            <string>MISRC</string>
            <key>CFBundleIdentifier</key>
            <string>dev.misrc.gui</string>
            <key>CFBundleVersion</key>
            <string>__BUILD_VERSION__</string>
            <key>CFBundleShortVersionString</key>
            <string>__BUILD_VERSION__</string>
            <key>CFBundlePackageType</key>
            <string>APPL</string>
            <key>CFBundleExecutable</key>
            <string>MISRC</string>
            <key>CFBundleIconFile</key>
            <string>MISRC</string>
            <key>LSMinimumSystemVersion</key>
            <string>10.15</string>
          </dict>
          </plist>
          EOF
          sed -i '' "s/__BUILD_VERSION__/${BUILD_VERSION}/g" "$APP_ROOT/Info.plist"


          shopt -s nullglob
          is_system_lib() {
            case "$1" in
              /System/*|/usr/lib/*) return 0 ;;
              *) return 1 ;;
            esac
          }

          resolve_dep_path() {
            local dep="$1"
            if [[ "$dep" == @rpath/* ]]; then
              local name="${dep#@rpath/}"
              local candidates=(
                "$DEPS_PREFIX/lib/$name"
                "$DEPS_PREFIX/lib64/$name"
                "$BREW_PREFIX/lib/$name"
                $BREW_PREFIX/opt/*/lib/"$name"
              )
              local candidate
              for candidate in "${candidates[@]}"; do
                if [[ -f "$candidate" ]]; then
                  printf '%s\n' "$candidate"
                  return 0
                fi
              done
              return 1
            fi
            printf '%s\n' "$dep"
          }

          collect_dep_tree() {
            local dep="$1"
            if is_system_lib "$dep"; then
              return 0
            fi
            local resolved
            resolved="$(resolve_dep_path "$dep")" || {
              echo "Unable to resolve dylib dependency: $dep" >&2
              return 1
            }
            local base
            base="$(basename "$resolved")"
            if [[ ! -f "$FW_DIR/$base" ]]; then
              cp "$resolved" "$FW_DIR/$base"
              chmod +w "$FW_DIR/$base"
              install_name_tool -id "@rpath/$base" "$FW_DIR/$base"
              while IFS= read -r subdep; do
                collect_dep_tree "$subdep"
              done < <(otool -L "$FW_DIR/$base" | tail -n +2 | awk '{print $1}')
            fi
          }

          patch_macho_refs() {
            local target="$1"
            chmod +w "$target" || true
            while IFS= read -r dep; do
              if is_system_lib "$dep"; then
                continue
              fi
              local base
              if [[ "$dep" == @rpath/* ]]; then
                base="$(basename "${dep#@rpath/}")"
              else
                base="$(basename "$dep")"
              fi
              if [[ -f "$FW_DIR/$base" ]]; then
                install_name_tool -change "$dep" "@rpath/$base" "$target" || true
              fi
            done < <(otool -L "$target" | tail -n +2 | awk '{print $1}')
          }

          for BIN in "$APP_ROOT/MacOS/MISRC" "$APP_ROOT/MacOS/misrc_capture" "$APP_ROOT/MacOS/misrc_extract"; do
            while IFS= read -r dep; do
              collect_dep_tree "$dep"
            done < <(otool -L "$BIN" | tail -n +2 | awk '{print $1}')
          done

          for BIN in "$APP_ROOT/MacOS/MISRC" "$APP_ROOT/MacOS/misrc_capture" "$APP_ROOT/MacOS/misrc_extract"; do
            install_name_tool -add_rpath "@executable_path/../Frameworks" "$BIN" 2>/dev/null || true
            patch_macho_refs "$BIN"
          done
          for DYLIB in "$FW_DIR"/*.dylib; do
            patch_macho_refs "$DYLIB"
          done

          # RTL-SDR must be bundled, not loaded from the build runner's Homebrew.
          RTLSDR_DYLIB="$(otool -L "$APP_ROOT/MacOS/MISRC" | awk '/@rpath\/librtlsdr.*\.dylib/ {print $1; exit}')"
          test -n "$RTLSDR_DYLIB"
          test -f "$FW_DIR/${RTLSDR_DYLIB#@rpath/}"

          if otool -L "$APP_ROOT/MacOS/MISRC" | grep -E '/opt/homebrew|/usr/local/opt'; then
            echo "Found non-portable Homebrew dylib path in MISRC binary" >&2
            exit 1
          fi
          if otool -L "$APP_ROOT/MacOS/misrc_capture" | grep -E '/opt/homebrew|/usr/local/opt'; then
            echo "Found non-portable Homebrew dylib path in misrc_capture binary" >&2
            exit 1
          fi

          "$APP_ROOT/MacOS/MISRC" --smoke-test
          set +e
          "$APP_ROOT/MacOS/misrc_capture" -h > /tmp/macos_bundle_capture_help.txt 2>&1
          RC_CAPTURE=$?
          "$APP_ROOT/MacOS/misrc_extract" -h > /tmp/macos_bundle_extract_help.txt 2>&1
          RC_EXTRACT=$?
          set -e
          test "$RC_CAPTURE" -eq 1
          test "$RC_EXTRACT" -eq 1
          grep -qi "Usage" /tmp/macos_bundle_capture_help.txt
          grep -qi "Usage" /tmp/macos_bundle_extract_help.txt

          codesign --force --deep --sign - dist/MISRC.app
          echo "$BUILD_VERSION" > dist/BUILD_VERSION.txt

      - name: Upload macOS architecture stage
        uses: actions/upload-artifact@v7
        with:
          name: macos-app-stage-${{ matrix.arch }}
          path: |
            dist/MISRC.app
            dist/BUILD_VERSION.txt
          if-no-files-found: error

  macos-app-universal:
    name: macOS APP build (universal)
    runs-on: macos-14
    needs:
      - macos-app-build
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Download arm64 stage
        uses: actions/download-artifact@v8
        with:
          name: macos-app-stage-arm64
          path: stage-arm64

      - name: Download x86_64 stage
        uses: actions/download-artifact@v8
        with:
          name: macos-app-stage-x86_64
          path: stage-x86_64

      - name: Merge universal app and package DMG
        id: package
        run: |
          set -eux
          BUILD_VERSION="$(cat stage-arm64/BUILD_VERSION.txt)"
          echo "version=$BUILD_VERSION" >> "$GITHUB_OUTPUT"

          mkdir -p dist
          cp -R stage-arm64/MISRC.app dist/MISRC.app

          while IFS= read -r rel; do
            ARM_FILE="stage-arm64/${rel}"
            X86_FILE="stage-x86_64/${rel}"
            OUT_FILE="dist/${rel}"
            if file "$ARM_FILE" | grep -q "Mach-O"; then
              lipo -create "$ARM_FILE" "$X86_FILE" -output "$OUT_FILE"
            else
              cp "$ARM_FILE" "$OUT_FILE"
            fi
          done < <(cd stage-arm64 && find MISRC.app -type f | sort)
          chmod +x dist/MISRC.app/Contents/MacOS/MISRC dist/MISRC.app/Contents/MacOS/misrc_capture dist/MISRC.app/Contents/MacOS/misrc_extract

          codesign --force --deep --sign - dist/MISRC.app

          LIPO_ARCHS="$(lipo -archs dist/MISRC.app/Contents/MacOS/MISRC)"
          echo "$LIPO_ARCHS" | grep -q 'arm64'
          echo "$LIPO_ARCHS" | grep -q 'x86_64'

          for DYLIB in dist/MISRC.app/Contents/Frameworks/librtlsdr*.dylib; do
            lipo -verify_arch arm64 x86_64 "$DYLIB"
          done

          dist/MISRC.app/Contents/MacOS/MISRC --smoke-test
          set +e
          dist/MISRC.app/Contents/MacOS/misrc_capture -h > /tmp/macos_universal_capture_help.txt 2>&1
          RC_CAPTURE=$?
          dist/MISRC.app/Contents/MacOS/misrc_extract -h > /tmp/macos_universal_extract_help.txt 2>&1
          RC_EXTRACT=$?
          set -e
          test "$RC_CAPTURE" -eq 1
          test "$RC_EXTRACT" -eq 1
          grep -qi "Usage" /tmp/macos_universal_capture_help.txt
          grep -qi "Usage" /tmp/macos_universal_extract_help.txt
          DMG_STAGE_DIR="dmg-stage"
          rm -rf "$DMG_STAGE_DIR"
          mkdir -p "$DMG_STAGE_DIR"
          cp -R dist/MISRC.app "$DMG_STAGE_DIR/MISRC.app"
          ln -s /Applications "$DMG_STAGE_DIR/Applications"
          DMG_NAME="macOS_MISRC_GUI_${BUILD_VERSION}_universal.dmg"
          hdiutil create \
            -volname "MISRC" \
            -srcfolder "$DMG_STAGE_DIR" \
            -ov \
            -format UDZO \
            "$DMG_NAME"
          test -f "$DMG_NAME"

      - name: Upload macOS DMG
        uses: actions/upload-artifact@v7
        with:
          name: macos-app
          path: macOS_MISRC_GUI_*_universal.dmg
          if-no-files-found: error

  release:
    name: Publish release assets
    runs-on: ubuntu-24.04
    needs:
      - linux-appimage
      - windows-exe
      - windows-exe-arm64
      - macos-app-universal
      - android-apk
    if: startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && github.event.inputs.create_release == 'true')
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: release-assets

      - name: Resolve release tag
        id: tag
        run: |
          set -eux
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            TAG="${GITHUB_REF#refs/tags/}"
          else
            TAG="${{ github.event.inputs.release_tag }}"
            if [[ -z "$TAG" ]]; then
              echo "release_tag is required when create_release=true" >&2
              exit 1
            fi
            if ! git rev-parse "refs/tags/$TAG" >/dev/null 2>&1; then
              git config user.name "github-actions[bot]"
              git config user.email "github-actions[bot]@users.noreply.github.com"
              git tag "$TAG" "$GITHUB_SHA"
              git push origin "$TAG"
            fi
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Assert release assets are tag-named (no dev leak)
        run: |
          set -eu
          TAG="${{ steps.tag.outputs.tag }}"
          shopt -s globstar nullglob
          artifacts=(
            release-assets/**/*_MISRC_GUI_*.zip
            release-assets/**/*_MISRC_GUI_*.dmg
            release-assets/**/*_MISRC_GUI_*.apk
          )
          if [[ "${#artifacts[@]}" -eq 0 ]]; then
            echo "ERROR: no release artifacts found in release-assets/" >&2
            exit 1
          fi
          for f in "${artifacts[@]}"; do
            base="$(basename "$f")"
            case "$base" in
              *_MISRC_GUI_dev-*)
                echo "ERROR: release artifact '$base' is dev-named; refusing to publish under tag $TAG" >&2
                exit 1
                ;;
              *"$TAG"*) : ;;
              *)
                echo "ERROR: release artifact '$base' does not contain the release tag $TAG" >&2
                exit 1
                ;;
            esac
          done
          echo "All ${#artifacts[@]} release assets are tag-named ($TAG)."

      - name: Build release summary
        id: summary
        run: |
          set -eu
          TAG="${{ steps.tag.outputs.tag }}"
          # Find the previous tag for the Highlights log range.
          PREV_TAG="$(git describe --tags --abbrev=0 "${TAG}^" 2>/dev/null || true)"
          if [[ -n "$PREV_TAG" ]]; then
            LOG_RANGE="${PREV_TAG}..${TAG}"
          else
            LOG_RANGE="${TAG}"
          fi
          # Build the structured release body. generate_release_notes: true
          # appends GitHub's auto-generated What's Changed PR list after this
          # body, matching the v1.1.0 release format. Write each line with
          # printf to avoid heredoc backtick-escaping issues.
          BODY_FILE="/tmp/release_body.md"
          : > "$BODY_FILE"
          printf '%s\n' '' '## Highlights' '' >> "$BODY_FILE"
          git log --pretty=format:'- %s (%h)' --no-merges "$LOG_RANGE" 2>/dev/null >> "$BODY_FILE" || true
          if [[ "$(tail -1 "$BODY_FILE")" == '## Highlights' ]]; then
            printf '%s\n' "- (no commits found in range ${LOG_RANGE})" >> "$BODY_FILE"
          fi
          # Contributors as GitHub @handles (de-duplicated). Handles are
          # derived from commit author emails: noreply github emails resolve
          # automatically (<handle>@users.noreply.github.com or
          # <id>+<handle>@users.noreply.github.com), and an explicit
          # email=handle map covers non-noreply emails. Commits whose email
          # can't be resolved are skipped (their work is still credited via
          # Highlights + GitHub's auto-generated What's Changed). No author
          # emails or raw names are published.
          CONTRIBUTOR_HANDLE_MAP="
            harry@opcomedia.com=harrypm
          "
          github_handle_for_email() {
            local email="$1"
            while IFS='=' read -r map_email map_handle; do
              map_email="${map_email// /}"; map_handle="${map_handle// /}"
              [[ -z "$map_email" ]] && continue
              if [[ "${email,,}" == "${map_email,,}" ]]; then
                printf '%s' "$map_handle"
                return 0
              fi
            done <<< "$CONTRIBUTOR_HANDLE_MAP"
            case "$email" in
              *"+*"@users.noreply.github.com) printf '%s' "${email%%+*}"; return 0 ;;
              *@users.noreply.github.com)     printf '%s' "${email%@users.noreply.github.com}"; return 0 ;;
            esac
            return 1
          }
          printf '\n%s\n' '' '## Contributors' '' >> "$BODY_FILE"
          CONTRIB_HANDLES="$(git log --pretty=format:'%ae' --no-merges "$LOG_RANGE" 2>/dev/null | sort -u | while IFS= read -r email; do
            handle="$(github_handle_for_email "$email")" && printf '%s\n' "@$handle"
          done | sort -u)"
          if [[ -n "$CONTRIB_HANDLES" ]]; then
            while IFS= read -r h; do
              [[ -n "$h" ]] || continue
              printf -- '- %s\n' "$h" >> "$BODY_FILE"
            done <<< "$CONTRIB_HANDLES"
          else
            printf '%s\n' "- (no contributors found in range ${LOG_RANGE})" >> "$BODY_FILE"
          fi
          printf '\n%s\n' '' '## Downloads' '' >> "$BODY_FILE"
          printf '| Platform | x86_64 (Intel / AMD) | arm64 (Apple Silicon / aarch64) | Android (arm64-v8a APK) |\n' >> "$BODY_FILE"
          printf '|---|---|---|---|\n' >> "$BODY_FILE"
          DL_BASE="https://github.com/harrypm/MISRC-GUI/releases/download/${TAG}"
          WIN_X86="Windows_MISRC_GUI_${TAG}_x86.zip"
          WIN_ARM="Windows_MISRC_GUI_${TAG}_arm64.zip"
          MAC_DMG="macOS_MISRC_GUI_${TAG}_universal.dmg"
          LIN_X86="Linux_MISRC_GUI_${TAG}_x86.zip"
          LIN_ARM="Linux_MISRC_GUI_${TAG}_arm64.zip"
          APK_ARM64="Android_MISRC_GUI_${TAG}_arm64.apk"
          printf '| Windows 10/11 | [%s](%s/%s) | [%s](%s/%s) | - |\n' "$WIN_X86" "$DL_BASE" "$WIN_X86" "$WIN_ARM" "$DL_BASE" "$WIN_ARM" >> "$BODY_FILE"
          printf '| macOS 11+ | [%s](%s/%s) | [%s](%s/%s) | - |\n' "$MAC_DMG" "$DL_BASE" "$MAC_DMG" "$MAC_DMG" "$DL_BASE" "$MAC_DMG" >> "$BODY_FILE"
          printf '| Linux (Ubuntu 22.04+) | [%s](%s/%s) | [%s](%s/%s) | - |\n' "$LIN_X86" "$DL_BASE" "$LIN_X86" "$LIN_ARM" "$DL_BASE" "$LIN_ARM" >> "$BODY_FILE"
          printf '| Android 11+ | - | - | [%s](%s/%s) |\n' "$APK_ARM64" "$DL_BASE" "$APK_ARM64" >> "$BODY_FILE"
          # GitHub admonition: the > [!NOTE] line MUST be immediately followed
          # by the > content lines with NO blank line between them, or GitHub
          # renders it as plain quoted text instead of a note box. Do NOT use
          # printf '\n%s\n' with multiple args here — printf reuses the format
          # per arg, which inserts a blank line after [!NOTE] and breaks the box.
          printf '\n%s\n' '> [!NOTE]' >> "$BODY_FILE"
          printf '%s\n' '> The arm64 Windows build is untested and considered experimental.' >> "$BODY_FILE"
          printf '%s\n' '> Android support is very alpha and experimental.' >> "$BODY_FILE"
          # Write multi-line output to GITHUB_OUTPUT.
          {
            echo "body<<BODY_EOF"
            cat "$BODY_FILE"
            echo "BODY_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: MISRC GUI ${{ steps.tag.outputs.tag }}
          body: ${{ steps.summary.outputs.body }}
          generate_release_notes: true
          files: |
            release-assets/**/Linux_MISRC_GUI_*_x86.zip
            release-assets/**/Linux_MISRC_GUI_*_arm64.zip
            release-assets/**/Windows_MISRC_GUI_*_x86.zip
            release-assets/**/Windows_MISRC_GUI_*_arm64.zip
            release-assets/**/macOS_MISRC_GUI_*_universal.dmg
            release-assets/**/Android_MISRC_GUI_*_arm64.apk
